#include "util.h"
void init(int* marks, int size, int min_mark, int max_mark) {
	for (int i = 0; i < SIZE; i++)
	{
		marksA[i] = rand() % (MAX_MARK-MIN_MARK + 1);

	}
}

string convert(int* marks, int size) {
	string s = 0;
}